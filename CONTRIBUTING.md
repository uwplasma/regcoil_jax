# Contributing to regcoil_jax

- Development install: `pip install -e '.[dev]'`
- Run tests: `pytest -q`
- Build docs: `python -m sphinx -b html docs docs/_build/html -q`
- Update Fortran parity baselines: `python scripts/generate_fortran_reference_outputs.py`

More details live in `docs/contributing.rst`.

